package com.github.the20login.trivial.bankng.processing;

public enum OperationErrorCode {
    SELF_TRANSFER,
    ACCOUNT_NOT_FOUND,
    INSUFFICIENT_FUNDS,
    INCORRECT_AMOUNT,
    DUPLICATE_RECORD,
    VALIDATION_ERROR
}
